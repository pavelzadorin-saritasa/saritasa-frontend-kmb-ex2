/**
 * User dto.
 */
export interface UserDto {

  /** Unique identifier. */
  readonly id: number;

  /** First name. */
  readonly first_name: string;

  /** Last name. */
  readonly last_name: string;
}
